{% extends 'layout.html.twig' %}

{% block content %}
    <img src="/assets/images/road66.jpg" alt="landscape road 66">
{# Présentation #}
    <h1 class="titleAssociation">L'ASSOCIATION EN BREF ...</h1>
    <div class="container-fluid">
        <div class="row d-flex justify-content-center">
            <div class="imagePresentation">
                <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/6/69/Country-Dancing-Arthur_Murray_Dance_Studio_in_The_Woodlands_TX1080x720.jpg/800px-Country-Dancing-Arthur_Murray_Dance_Studio_in_The_Woodlands_TX1080x720.jpg" class="card-img-top imgPres" alt="...">
            </div>
            <div class="jumbotron jumbotron-fluid bg-white">
                <div class="container">
                    <p class="lead">
                        Country Club Route 45 est un club de danse country et  de line dance de la région orléanaise, une association née en 2007
                        du désir de partager la passion de danser dans le respect des traditions et également avec la joie que ce hobby peut apporter
                        à un groupe de danseurs qui ont les mêmes goûts. L'association a toujours aujourd'hui un but simple : notre idéal est la promotion
                        de la danse country et de la line dance, et le partage de notre passion. Le défunt Robert Wanstreet, fondateur de la danse country en France a accepté
                        d'être le parrain du club dès sa création, chose qu'il n'avait jamais faite auparavant.
                    </p>
                </div>
            </div>
        </div>
    </div>


{# Animatrices #}
    <h1>Les animatrices</h1>
    <div class="container-fluid teachers">
        {% for teacher in teachers %}
            <div class="card teacherCard">
                <img src="/assets/upload/{{ teacher.image }}" class="card-img-top imgTeachers" alt="Country's teacher">
                <div class="card-body">
                    <h2 clas="teacherTitle">{{ teacher.firstname }} {{ teacher.lastname }}</h2>
                    <p class="card-text teacher-text">{{ teacher.description }}</p>
                </div>
            </div>
        {% endfor %}
    </div>




{# Bureau #}

    <h1 class="titleMember">Les membres du bureau</h1>
    <div class="container-fluid">
        <div class="row d-flex justify-content-around">
            {% for member in members %}
                <div class="cardMember" style="width: 18rem;">
                    <img src="/assets/upload/{{ member.picture }}" class="card-img-top imgMember" alt="members photos">
                    <div class="card-body">
                        <h5 class="card-title memberName">{{ member.firstname }} {{ member.lastname }}</h5>
                        <p class="card-text memberFunction">{{member.function}}</p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}